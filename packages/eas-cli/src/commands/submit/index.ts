import { Command } from '@oclif/command';

export default class SubmitIndex extends Command {
  static description = 'upload app binary to app store';

  async run() {
    throw new Error('Not implemented');
  }
}
