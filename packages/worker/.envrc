#!/usr/bin/env bash

export ENVIRONMENT="development"
export PORT="3005"
export WORKER_PORT="3005"
export WORKER_IP="localhost"
export LOGGER_LEVEL="debug"

source_url "https://raw.githubusercontent.com/expo/expo.nix/refs/tags/v1.0.1/direnvrc" "sha256-WLwFio6Siu4hFxN0APXqUzlFSmePR9leIKTz/sON52k="

if has nix; then
  use flake
else
  unset _VOLTA_TOOL_RECURSION
fi
